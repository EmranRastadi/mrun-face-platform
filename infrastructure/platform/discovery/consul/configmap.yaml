apiVersion: v1
kind: ConfigMap

metadata:
  name: consul-config
  namespace: mrun-system

data:
  consul.hcl: |
    server = true

    bootstrap_expect = 1

    ui_config {
      enabled = true
    }

    client_addr = "0.0.0.0"

    data_dir = "/consul/data"

    log_level = "INFO"