apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: mrun-database
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/component: external-service
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP