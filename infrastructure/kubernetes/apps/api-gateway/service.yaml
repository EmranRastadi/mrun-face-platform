apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: mrun-apps
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 80
      name: http
      targetPort: 3000
      nodePort: 30080