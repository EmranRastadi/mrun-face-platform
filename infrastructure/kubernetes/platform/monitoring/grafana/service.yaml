apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: mrun-monitoring
  labels:
    app: grafana
spec:
  type: ClusterIP      # یا NodePort / LoadBalancer
  ports:
    - port: 3000
      targetPort: 3000
      name: grafana-http
  selector:
    app: grafana