apiVersion: v1
kind: ConfigMap

metadata:
  name: postgres-config
  namespace: mrun-system

data:
  POSTGRES_DB: 'users'

  PGDATA: '/var/lib/postgresql/data/pgdata'

  POSTGRES_PORT: "5432"

  TZ: 'Asia/Tehran'

  PGTZ: 'Asia/Tehran'

  POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C.UTF-8"