apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser

metadata:
  name: api-gateway
  namespace: mrun-messaging
  labels:
    strimzi.io/cluster: kafka

spec:
  authentication:
    type: scram-sha-512